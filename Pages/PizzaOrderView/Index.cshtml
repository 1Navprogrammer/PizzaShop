@page
@model PizzaShop.Pages.PizzaOrderView.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PizzaOrder[0].Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PizzaOrder[0].Pizza)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PizzaOrder[0].Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PizzaOrder) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Order.OrderNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pizza.PizzaCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PizzaOrderId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PizzaOrderId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PizzaOrderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
